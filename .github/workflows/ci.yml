name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run typecheck
      - name: DEBUG isolated daemon+ingest
        run: |
          bun --version
          bun test packages/daemon/test/daemon.test.ts packages/cli/test/ingest.test.ts 2>&1 | tail -5
      - run: bun test
      - name: Graph integrity (litopys check)
        run: |
          mkdir -p /tmp/litopys-ci-graph
          LITOPYS_GRAPH_PATH=/tmp/litopys-ci-graph \
            bun packages/cli/src/index.ts check --json
