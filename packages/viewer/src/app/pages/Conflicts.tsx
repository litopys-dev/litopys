import { ComingSoon } from "../components/ComingSoon.tsx";

export default function Conflicts() {
  return (
    <ComingSoon
      title="Conflicts"
      phase="Phase 4"
      description="Similarity-detected duplicate candidates, merge proposals, schema-level conflicts. Accept or reject from the UI."
    />
  );
}
